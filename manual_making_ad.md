# 광고 메이커 사용 설명서 (`/ad`)

페이지 조립형 제품 광고 숏폼(9:16, 1080×1920, 30fps)을 만드는 도구입니다. 한 편의 광고를 **페이지(beat)의 연속**으로 구성하고, 각 페이지에 비주얼·모션·전환 템플릿과 자막·내레이션을 채운 뒤, 대본(LLM) → TTS → MP4 렌더의 3단계 파이프라인으로 완성합니다. 비교 숏폼 위자드(`/`)와 별개로 공존하는 워크스트림입니다.

---

## 0. 사전 준비 (환경 변수)

`.env`(또는 `.env.local`)에 키를 채웁니다. `.env.example` 참고.

| 변수 | 용도 | 필수 여부 |
|------|------|-----------|
| `OPENROUTER_API_KEY` | 대본 생성 LLM (OpenRouter 경유) | **대본 생성에 필수** |
| `AD_COMPOSE_MODEL` | 대본 모델. 기본값 `openai/gpt-4.1` | 선택 |
| `ELEVENLABS_API_KEY` | 내레이션(VO) TTS | **TTS 단계에 필수** |
| `ELEVENLABS_VOICE_ID` | 보이스. 기본값 `Sarah`(무료 티어 사용 가능). 라이브러리 보이스는 유료 플랜 필요 | 선택 |
| `FAL_API_KEY` / `WAVESPEED_API_KEY` | 페이지 소스용 AI 이미지/영상 생성 모델 | AI 이미지 생성 시 |

> 렌더링은 Remotion이 자체 ffmpeg를 번들하므로 시스템에 ffmpeg/ffprobe를 따로 설치할 필요가 없습니다.

개발 서버 실행 후 브라우저에서 **`/ad`** 로 접속합니다. (예: `http://localhost:3000/ad`) 우상단 `← 비교 숏폼` 링크로 위자드(`/`)와 오갈 수 있습니다.

---

## 1. 새 광고 만들기

`/ad` 첫 화면에서 시작합니다.

1. **주제 입력** — 상단 입력칸에 직접 입력(예: `AI 영상 에디터`)하거나, 아래 **추천 주제(TopicSuggest)** 에서 트렌드 후보를 골라 자동 채웁니다. 주제는 대본 생성의 핵심 입력이며, 비워도 시작은 가능합니다.
2. **시작 방식 선택:**
   - **`9-beat 프리셋으로 시작`** — TapNow 스타일 9페이지 골격이 미리 깔린 프로젝트를 만듭니다. 가장 빠른 출발점입니다.
   - **`빈 프로젝트`** — 페이지 없이 시작. 대본 생성이나 수동 추가로 채웁니다.
3. 생성되면 곧바로 **에디터**로 진입합니다. 만들어진 프로젝트는 `outputs/results/ad/<타임스탬프>/`에 저장됩니다.

**최근 광고 프로젝트** 섹션에서 기존 프로젝트(페이지 수·생성 시각·렌더 여부 표시)를 클릭해 이어서 편집할 수 있습니다.

---

## 2. 에디터 화면 구성

에디터는 상단 헤더 + 파이프라인 바 + 3열 레이아웃 + 하단 패널로 구성됩니다.

- **헤더** — `← 목록`(저장 후 목록 복귀), 프로젝트 ID, 주제 입력칸, 우측 `저장 중…/저장됨` 상태. 편집은 **400ms 디바운스로 자동 저장**되므로 별도 저장 버튼이 없습니다.
- **파이프라인 바** — `① 대본 → ② TTS → ③ 렌더` 진행 상태와 실행 버튼.
- **3열:** 왼쪽 `페이지 목록` · 가운데 `페이지 편집(인스펙터)` · 오른쪽 `미리보기 + 렌더 패널`.
- **하단 3패널:** `제품 정보` · `BGM` · `엔드카드`.

> 대본 생성·TTS·렌더 같은 장시간 작업 중에는 다른 무거운 작업이 잠기고, 페이지를 떠나려 하면 경고가 뜹니다.

---

## 3. 제품 정보 입력 (먼저 채우는 걸 권장)

하단 **제품 정보** 패널. 이 값들이 대본 생성 프롬프트와 비주얼 템플릿·엔드카드에 사용됩니다.

- **제품명**
- **브랜드 컬러** — 비주얼 템플릿에서 강조색으로 사용 (기본 `#ff5a1f`)
- **한 줄 소개(oneLiner)** — 주제가 비어 있으면 대본 생성 시 주제 대신 쓰입니다.
- **가치 제안(valueProps)** — 줄바꿈으로 구분된 불릿
- **CTA** — 행동 유도 문구. 엔드카드에서 기본값으로 사용.

대본 품질을 위해 **대본 생성 전에 제품 정보를 먼저 채우는 것**이 좋습니다.

---

## 4. ① 대본 생성 (LLM)

파이프라인 바의 **`대본 생성`** 버튼을 누릅니다.

- 주제 + 제품 정보 + 템플릿 카탈로그를 LLM(`openai/gpt-4.1`)에 보내 **5~10페이지(총 30~45초) 분량의 페이지 배열**을 생성합니다. 각 페이지에는 sourceType, 비주얼/모션/전환 템플릿 ID, 자막(≤20자), 내레이션(VO), 이미지 프롬프트 또는 클립 검색어가 채워집니다.
- 대본 구조는 훅(문제 제기) → 제품 공개/데모 → 가치 제안 → 사회적 증거/결과 → 마무리 푸시 순으로 설계됩니다. VO는 존댓말(~요체), 과장·미검증 효능 주장·경쟁사 비방 금지로 작성됩니다.
- **이미 페이지가 있으면 확인 모달**이 뜹니다 — 대본 재생성은 **기존 페이지를 소스/VO 포함 전부 교체**합니다. 주의하세요.
- LLM이 모르는 템플릿 ID를 내놓으면 하드 실패 없이 **안전한 기본값으로 대체하고 한글 경고**를 보여줍니다(노란 배너). 페이지 수가 권장 범위보다 적어도 경고만 띄웁니다.

> 빈 프로젝트로 시작했어도 대본 생성으로 한 번에 골격을 채울 수 있습니다. 생성된 대본은 인스펙터에서 자유롭게 손봅니다.

---

## 5. 페이지 편집 (인스펙터)

왼쪽 **페이지 목록**에서 페이지를 선택하면 가운데 인스펙터가 열립니다. 목록에서 페이지 추가/삭제/순서 변경이 가능합니다.

### 5-1. 소스 (영상/이미지)

페이지 상단에서 **소스 타입**(이미지/영상)을 고른 뒤, 아래 탭에서 소스를 지정합니다:

- **AI 생성** (이미지 전용) — 이미지 모델 선택 + 프롬프트(영문 권장) 입력 후 `이미지 생성`. 결과가 페이지 소스로 설정됩니다.
- **업로드** — 로컬 이미지/영상 파일을 직접 올립니다.
- **소스 풀** — 프로젝트 전반에서 공유하는 에셋 풀(`HistoryPicker`)에서 기존 이미지/클립을 골라 씁니다.

소스 타입을 이미지↔영상으로 바꾸면 소스가 초기화되고, 현재 템플릿이 새 타입과 호환되지 않으면 기본 템플릿으로 자동 교체됩니다. 소스를 지정하지 않은 페이지는 렌더 시 그라데이션 플레이스홀더로 나옵니다.

### 5-2. 템플릿 3종 (비주얼 · 모션 · 전환)

각 페이지는 세 템플릿의 조합입니다. 드롭다운은 현재 소스 타입과 호환되는 항목만 보여줍니다.

**비주얼** — 화면 레이아웃:

| ID | 이름 | 설명 | 호환 |
|----|------|------|------|
| `fullscreen-title` | 풀스크린 타이틀 | 풀블리드 + 상단 대형 타이틀(훅/오프닝) | 이미지·영상 |
| `talking-head-caption` | 토킹헤드 자막 | 풀블리드 토킹헤드 영상 + 하단 자막 배너 | 영상 |
| `ui-demo-frame` | UI 데모 프레임 | 가짜 에디터 앱 창 안에 미디어(제품 데모) | 이미지·영상 |
| `canvas-grid` | 캔버스 그리드 | 노트 캔버스 + 2×2 콜라주(변형/갤러리) | 이미지 |
| `model-selector` | 모델 셀렉터 | 가짜 AI 모델 드롭다운 오버레이 | 이미지·영상 |
| `plain-caption` | 자막 배너 | 풀블리드 + 자막 배너 하나(범용 b-roll) | 이미지·영상 |
| `compare-2up` | 2분할 비교 | 서로 다른 **두 미디어(A vs B)** 를 상/하로 비교 + 가운데 VS 배지 | 이미지·영상 |

> **2분할 비교(`compare-2up`) 사용법:** 비주얼을 `2분할 비교`로 바꾸면 페이지 편집의 소스 영역이 **A(위) / B(아래)** 두 칸으로 나뉩니다. 각각 AI 생성·업로드·소스 풀에서 따로 채웁니다. B를 비워두면 아래 칸은 플레이스홀더로 렌더됩니다. (페이지당 소스가 2개가 되는 유일한 템플릿)

**모션** — 움직임:

| ID | 이름 | 설명 | 호환 |
|----|------|------|------|
| `none` | 정지 | 모션 없음 | 이미지·영상 |
| `ken-burns-zoom` | 켄번즈 줌 | 정지 이미지 느린 줌인 | 이미지 |
| `pan` | 패닝 | 정지 이미지 수평 패닝 | 이미지 |
| `shrink-into-ui` | UI로 축소 | 시그니처: 풀스크린 → UI 프레임으로 스프링 축소(제품 공개) | 이미지·영상 |
| `caption-pop` | 팝인 | 콘텐츠 스프링 팝인 | 이미지·영상 |

**전환** — *이 페이지에서 다음 페이지(또는 엔드카드)로 넘어갈 때* 효과:

| ID | 이름 | 설명 |
|----|------|------|
| `cut` | 컷 | 하드 컷(빠른 비트에 적합) |
| `fade` | 페이드 | 부드러운 크로스페이드 |
| `slide` | 슬라이드 | 다음 페이지가 오른쪽에서 슬라이드 |
| `wipe` | 와이프 | 방향성 와이프 |
| `zoom-blur` | 줌 블러 | 강한 줌블러 버스트(엔드카드 직전 권장) |
| `dreamy-zoom` | 드리미 줌 | 몽환적 회전 줌 블렌드 |

### 5-3. 자막 / 내레이션 (VO)

- **자막(caption)** — 화면에 표시되는 한글 텍스트, **20자 권장**(초과 시 경고색).
- **내레이션(VO)** — TTS로 읽히는 대본. 입력 후 `VO 생성`(또는 변경 후 `VO 재생성`)으로 개별 페이지 음성을 만들 수 있고, 생성되면 인라인 오디오 플레이어와 길이(초)가 표시됩니다.

### 5-4. 페이지 길이

- 기본 동작: **VO가 있으면 VO 길이에 자동 맞춤**, 없으면 기본 3초.
- 숫자를 입력하면 **수동 길이**로 고정됩니다(비우면 자동).
- 수동 길이가 VO보다 짧으면 ⚠️ 경고 — VO는 잘리지 않고 **다음 페이지로 이어져** 흐릅니다.

---

## 6. ② TTS 생성 (내레이션 음성)

ElevenLabs(`eleven_flash_v2_5`)로 VO 텍스트를 한국어 음성으로 합성합니다.

- **개별 생성:** 인스펙터의 `VO 생성` 버튼.
- **전체 생성:** 파이프라인 바의 **`TTS 생성 (전체)`** — VO가 있는 모든 페이지를 순차 처리하며 `TTS n/총개수…` 진행률을 표시합니다.
- **캐시/스킵:** 보이스ID + VO 텍스트 해시가 같으면 재생성하지 않습니다(멱등). 텍스트를 바꾸면 새로 만들고 오래된 음성 파일은 정리됩니다.
- 음성 파일은 `outputs/results/ad/<ts>/audio/`에 저장되고, 길이는 Remotion media-parser로 측정해 페이지 길이 자동 계산에 쓰입니다.

> TTS를 다시 누르기 전에 VO 텍스트 편집은 자동 저장(flush)됩니다 — 화면에 보이는 그대로 읽습니다.

---

## 7. BGM · 엔드카드

### BGM (하단 패널)

- **라이브러리** — `public/bgm/`의 트랙 선택(기본 `기본 트랙`) 또는 `사용 안 함`.
- **업로드** — mp3 직접 업로드.
- **볼륨/덕킹** — 기본 볼륨(기본 60%)과 VO가 나올 때 낮추는 **덕킹 볼륨**(기본 25%) 슬라이더.
- (음악 생성 탭은 API 연동 예정으로 비활성)

### 엔드카드 (하단 패널)

- **사용/미사용** 토글.
- **템플릿:** `logo-blur-in`(로고 블러인 + 한 줄 소개) / `logo-cta`(로고 + CTA 버튼).
- **진입 전환:** 마지막 페이지에서 엔드카드로 들어올 때 효과(`zoom-blur`/`dreamy-zoom` 권장).
- **길이(초)**, **CTA 문구**(비우면 제품 정보의 CTA 사용).

---

## 8. 미리보기

오른쪽 **미리보기**는 Remotion Player로 현재 프로젝트 상태(소스·템플릿·자막·전환·엔드카드)를 실시간 재생합니다. 렌더 없이 빠르게 확인하는 용도이며, 편집하면 즉시 반영됩니다.

---

## 9. ③ 렌더 (MP4)

오른쪽 **렌더 패널**(로컬 · 오디오 포함 · ko).

1. **`MP4 렌더`** 클릭. 진행 단계가 `브라우저 준비 → 번들링 → 렌더링 → 완료`로 % 표시됩니다(SSE 실시간).
2. 소스 없는 페이지가 있으면 ⚠️ "플레이스홀더로 렌더됩니다" 경고가 뜹니다.
3. 완료 시 결과 영상이 **렌더 히스토리**에 썸네일·생성시각과 함께 쌓이고, 미리보기/`다운로드`가 가능합니다.
4. 렌더 출력에는 **VO + BGM(덕킹 적용)** 오디오가 포함됩니다. (비교 숏폼과 달리 음소거가 아님)

렌더는 최신 저장본을 읽으므로, 누르기 전 편집 내용이 자동으로 flush됩니다.

---

## 10. 권장 작업 순서 (요약)

1. `.env`에 `OPENROUTER_API_KEY` · `ELEVENLABS_API_KEY` 설정
2. `/ad` → 주제 입력 → **9-beat 프리셋으로 시작**
3. 하단 **제품 정보** 채우기 (제품명·한 줄 소개·가치 제안·CTA·브랜드 컬러)
4. 파이프라인 **`대본 생성`** → 경고 배너 확인
5. 페이지별로 **소스 지정**(AI 생성/업로드/소스 풀) + 템플릿·자막·VO·길이 다듬기
6. **`TTS 생성 (전체)`**
7. **BGM**·**엔드카드** 설정
8. **미리보기**로 확인 → **`MP4 렌더`** → 다운로드

---

## 11. 문제 해결

| 증상 | 원인 / 해결 |
|------|-------------|
| 대본 생성 실패 | `OPENROUTER_API_KEY` 누락/오류. 모델은 `AD_COMPOSE_MODEL`로 변경 가능 |
| 템플릿 대체 경고(노란 배너) | LLM이 미존재 템플릿 ID를 생성 → 기본값으로 자동 대체됨. 인스펙터에서 원하는 템플릿으로 교체 |
| TTS 실패 | `ELEVENLABS_API_KEY` 누락, 또는 유료 보이스 ID를 무료 티어에서 사용. 기본 `Sarah` 보이스 사용 |
| AI 이미지 생성 안 됨 | `FAL_API_KEY`/`WAVESPEED_API_KEY` 누락, 또는 영상 페이지(AI 생성은 이미지 전용 — 영상은 업로드/소스 풀 사용) |
| 렌더에 페이지가 빈칸 | 소스 미지정 페이지(플레이스홀더). 소스를 지정 후 재렌더 |
| VO가 다음 페이지로 넘어감 | 수동 페이지 길이가 VO보다 짧음. 길이를 비워 자동(VO 맞춤)으로 두거나 늘림 |
| 저장 실패 메시지 | 디바운스 저장 실패 — 다시 수정하면 자동 재시도됨 |

---

## 12. 산출물 위치

- 프로젝트 데이터: `outputs/results/ad/<타임스탬프>/index.json`
- VO 음성: `outputs/results/ad/<타임스탬프>/audio/page-<id>-<hash>.mp3`
- 렌더 결과 MP4: 같은 프로젝트 디렉터리, 렌더 히스토리에서 다운로드

> 자세한 설계·확장(템플릿 추가 방법 등)은 저장소 루트의 `SHORTFORM_TEMPLATE_SYSTEM_V2.md` / `SHORTFORM_TEMPLATE_SYSTEM_V2_PLAN.md` 참고.
</content>
</invoke>
